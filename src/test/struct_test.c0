#use <conio>
#use <string>

struct test {
    int a;
    string s;
};

typedef struct test test_t;

int main() {
    test_t* str = alloc(test_t);
    str -> a = 10;
    str -> s = "Hi!";
    int a = str -> a;
    string s = str -> s;
    printf("{%d, %s}", a, s);
    return 0;
}
