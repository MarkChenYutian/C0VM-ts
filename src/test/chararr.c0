#use <conio>
#use <string>

int main() {
    string testStr = "test-file";
    int l = string_length(testStr);
    char[] arr = string_to_chararray(testStr);
    for (int i = 0; i < l; i ++) {
        printf("%c-", arr[i]);
    }
    return 0;
}

