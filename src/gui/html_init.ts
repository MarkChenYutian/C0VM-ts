import { editor_init } from "./code_editor_setup";

/**
 * Entry point of HTML/Javascript Initialization. Will be called when the DOM is loaded
 */
export default function html_init() {
    editor_init();
}
